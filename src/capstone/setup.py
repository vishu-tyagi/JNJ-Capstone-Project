import setuptools

setuptools.setup(
    name="capstone",
    version="0.0.1",
    description="Package for JNJ capstone project",
    packages=setuptools.find_packages(),
    python_requires=">=3.9"
)
